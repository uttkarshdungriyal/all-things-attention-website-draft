import Link from "next/link";
import { posts } from "./content";
export const metadata = { title: "GTM, Cold Email & Clay Guides | All Things Attention", description: "Practical guides for go-to-market strategy, cold email, and Clay workflows." };
export default function BlogIndex() { return <main className="subpage"><nav className="nav shell"><Link className="brand" href="/"><span className="brand-mark">A*</span><span>ALL THINGS<br/>ATTENTION</span></Link><Link className="button button-small" href="/#join">Join Discord ↗</Link></nav><section className="subhero shell"><p className="section-label">THE FIELD GUIDE · 50 ARTICLES</p><h1>Practical answers for<br/><em>attention systems.</em></h1><p>Evidence-minded playbooks for GTM, cold email, and Clay workflows. One useful question at a time, organized into three connected topic clusters.</p></section><section className="shell blog-grid">{posts.map(post=><article key={post.slug}><p className="section-label">{post.category} · {post.keyword}</p><h2><Link href={`/blog/${post.slug}`}>{post.title}</Link></h2><p>{post.summary}</p><Link className="text-link" href={`/blog/${post.slug}`}>Read guide →</Link></article>)}</section></main> }
