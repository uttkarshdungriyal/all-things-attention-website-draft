import { notFound } from "next/navigation";
import Link from "next/link";
import { posts, postBySlug } from "../content";

export function generateStaticParams() { return posts.map(({ slug }) => ({ slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const post = postBySlug((await params).slug); if (!post) return {}; return { title: `${post.title} | All Things Attention`, description: post.summary, keywords: [post.keyword, ...post.points], alternates: { canonical: `/blog/${post.slug}` } }; }

const paragraph = (post: NonNullable<ReturnType<typeof postBySlug>>, point: string, index: number) => `In a useful ${post.keyword} process, ${point.toLowerCase()} is not a box to tick; it is a decision that should be visible to the next person. Start with the smallest reliable input, write down what you believe, and choose an action that can prove or disprove it. For example, a team might review one segment, one message, or one record before applying the same rule to a whole list. This keeps the work reversible and makes mistakes cheap to find. It also gives you a clear explanation when a stakeholder asks why a prospect, channel, or workflow was included. The practical habit is to document the source, owner, date, and confidence level. Then review the outcome after a defined window rather than changing several variables at once. ${index % 2 ? "Quality matters more than apparent volume: a smaller set of useful conversations is a stronger signal than a large set of ignored activity." : "A human review step is especially important when automation or AI is involved, because plausible output is not the same thing as verified evidence."}`;

export default async function BlogPost({ params }: { params: Promise<{ slug: string }> }) {
  const post = postBySlug((await params).slug); if (!post) notFound();
  const related = posts.filter((p) => p.category === post.category && p.slug !== post.slug).slice(0, 4);
  const faq = [{ q: `What is ${post.keyword}?`, a: post.summary }, { q: `How should a team start with ${post.keyword}?`, a: "Choose one audience, one workflow, and one measurable next action. Review the evidence before scaling." }];
  const jsonLd = { "@context": "https://schema.org", "@type": "Article", headline: post.title, description: post.summary, datePublished: "2026-09-09", dateModified: "2026-09-10", author: { "@type": "Organization", name: "All Things Attention" }, mainEntityOfPage: `https://allthingsattention.com/blog/${post.slug}`, about: post.keyword };
  const faqLd = { "@context": "https://schema.org", "@type": "FAQPage", mainEntity: faq.map(item => ({ "@type": "Question", name: item.q, acceptedAnswer: { "@type": "Answer", text: item.a } })) };
  return <main className="subpage"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}/><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqLd) }}/><nav className="nav shell"><Link className="brand" href="/"><span className="brand-mark">A*</span><span>ALL THINGS<br/>ATTENTION</span></Link><Link className="button button-small" href="/#join">Join Discord ↗</Link></nav><article className="shell article"><p className="section-label">{post.category} · {post.keyword}</p><h1>{post.title}</h1><p className="article-meta">By All Things Attention · Published September 9, 2026 · Updated September 10, 2026 · 8 min read</p><p className="article-lede">{post.summary} This evidence-minded guide explains the concept, shows a repeatable workflow, and gives you a safe way to put it into practice.</p><h2>Quick answer</h2><p>{post.title.replace("?", "")} is a repeatable way to turn an uncertain growth task into a clear decision. The best starting point is narrow: define the audience or input, describe the problem, choose one action, and measure the quality of what happens next. Do not promise a result you have not verified. Instead, use a small experiment and keep the context needed to interpret the outcome.</p><h2>Why this matters</h2><p>Teams often jump to a tool, channel, prompt, or template before they agree on the job to be done. That creates activity without learning. A durable system makes assumptions explicit, assigns ownership, and leaves a record another person can understand. It also protects trust: people can see what data was used, why a recommendation was made, and how to opt out or correct an error.</p><h2>The framework</h2><ol>{post.points.map((point, index) => <li key={point}><strong>{point}.</strong> {paragraph(post, point, index)}</li>)}</ol><h2>Step-by-step workflow</h2><p><strong>1. Define the input.</strong> Write one sentence describing the audience, account, message, or dataset. If two people interpret it differently, narrow it before continuing.</p><p><strong>2. Set a learning goal.</strong> Decide what evidence would change your mind. A useful goal is observable and time-bound, not a vanity metric.</p><p><strong>3. Run a small test.</strong> Work on a sample that a human can inspect. Keep the original inputs and note any exclusions.</p><p><strong>4. Review quality.</strong> Check accuracy, relevance, consent, and whether the output is useful to its recipient. Record false positives and false negatives.</p><p><strong>5. Document the next iteration.</strong> Keep the winning rule, the caveats, and the owner in a shared place. A workflow is maintainable only when someone can pick it up later.</p><h2>A practical example</h2><div className="example"><p><strong>Scenario:</strong> A small B2B team wants to test one focused segment without overbuilding a database.</p><p><strong>Input:</strong> ten reviewed companies that match the documented criteria.</p><p><strong>Action:</strong> apply one relevant message or workflow change, with a clear opt-out and a human approval step.</p><p><strong>Review:</strong> record positive replies, useful objections, data issues, and what should not be repeated. This is an illustrative process, not a claimed performance result.</p></div><h2>Implementation notes</h2>{implementationNotes.map(note=><p key={note}>{note}</p>)}<h2>Common mistakes</h2><ul><li>Changing the audience, offer, channel, and copy at the same time.</li><li>Treating an unverified data point or AI-generated sentence as fact.</li><li>Optimizing for volume when relevance and reply quality are the real constraints.</li><li>Skipping consent, privacy, authentication, or opt-out requirements.</li><li>Failing to record exclusions, assumptions, and the date of the research.</li></ul><h2>Frequently asked questions</h2>{faq.map(item=><details key={item.q}><summary>{item.q}<span>+</span></summary><p>{item.a}</p></details>)}<p className="article-end">Join All Things Attention for practical discussions on GTM, content, distribution, and automation.</p><h2>Keep reading</h2><div className="related">{related.map(item=><Link key={item.slug} href={`/blog/${item.slug}`}>{item.title} →</Link>)}</div></article></main>;
}
const implementationNotes = ["Make the first version easy to inspect. Use a simple table or document with columns for the input, source, decision, owner, and next review date. If a field is unknown, record unknown instead of guessing.","Separate research from execution. Research should produce a short, reviewable brief; execution should use only approved facts in that brief. This separation makes stale information and sensitive data easier to catch.","Choose metrics that reflect the job. For a message, that may be useful replies rather than opens. For a workflow, it may be the percentage of records that pass review. Always record the denominator and time window.","Plan a stop condition before you begin. Stop or revise when data is uncertain, someone opts out, a source is unavailable, or the cost is greater than the likely learning. Responsible systems make stopping explicit.","Schedule maintenance. Audiences, roles, websites, and tools change. Review the workflow at a predictable cadence and preserve a short changelog before deciding that a tactic worked or failed."];
